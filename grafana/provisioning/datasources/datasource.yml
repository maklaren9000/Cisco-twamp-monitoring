apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://172.17.0.1:9090  # Этот IP доступен из любого контейнера (сеть докера)
                                 # This IP is accessible from any container (docker network)
    isDefault: true              
    editable: true
